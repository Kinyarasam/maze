#include "../game.h"

/**
 * _isupper - check for upper_case letter
 * @c: character to check the case
 *
 * Return: Always 0.(Success)
 *	otherwise: Always 1.
 */
int _isupper(int c)
{
	return (c > 65 && c <= 90);
}
